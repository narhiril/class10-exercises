@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h1>Events:</h1>
<p>
    <a asp-controller="Events" asp-action="Add">Add Event</a>
</p>

<div>
@if(ViewBag.Events?.Count > 0 ?? false)
{
    <table class='table'>
        <thead>
            <tr>
                <th>Name</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>

    @foreach (KeyValuePair<string, string> entry in ViewBag.Events)
    {
        <tr>
            <td>@entry.Key</td>
            <td>@entry.Value</td>
        </tr>    
    }
    </tbody>
    </table>
}
else
{
    <p>No events found!</p>
}
</div>